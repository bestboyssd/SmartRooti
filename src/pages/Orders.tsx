import React from 'react';

const Orders = () => {
    return (
        <div>
            <h1>Orders History</h1>
            <p>This will contain the user's order history.</p>
        </div>
    );
};

export default Orders;